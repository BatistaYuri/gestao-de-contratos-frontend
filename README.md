# Gestão de Contratos — Frontend

Base do frontend criada com React, TypeScript e Vite.

```bash
npm install
npm run dev
```

Durante o desenvolvimento, requisições para `/api` são encaminhadas para
`http://localhost:3001`. A URL base pode ser configurada com `VITE_API_URL`.
